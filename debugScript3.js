const { InMemorySessionService, Runner } = require('@google/adk');
const { visionIntakeAgent } = require('./.next/server/app/api/analyze/route.js'); // Cannot easily import TS Next.js module here, so I'll just post.
