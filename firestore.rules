rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-side only — all access via Firebase Admin SDK
    // No client-side reads/writes
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
